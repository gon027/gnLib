#include "App.h"

/*
#include "Texture.h"
#include "Sprite.h"
#include "StringRender.h"
#include "Color.h"

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrefInstance, LPSTR lpCmdLine, int nCmdShow) {
	App app{ "Test_StringRender" };

	StringRender sr;
	sr.create(app.getGraphics());

	while (app.doEvent()) {
		app.begin();

		auto c = Color(255, 0, 255);

		sr.drawText("aaaaa", 0, 0, c);
		sr.drawText("bbbbbb", 0, 20, Color(200, 135, 14));
		sr.drawText("ABCVDFG", 0,40, Color(200, 200, 9));

		app.end();
	}
}*/