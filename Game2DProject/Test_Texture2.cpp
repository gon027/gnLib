#include "gnLib.h"

/*
void gnMain() {
	App app;

	
	TextureTest* t = new TextureTest("img/char.png");
	t->test();
	umImpl::gnSprite gs;
	gs.setTexture(t);

	while (app.doEvent()) {
		app.begin();

		gs.draw({ 100, 100 });

		Debug::drawFormatText(0, 20, Color::Black, "%d", t->geteSize().getWidth());
		Debug::drawFormatText(0, 40, Color::Black, "%d", t->geteSize().getHeight());

		app.end();
	}

	delete t;
}
*/